# 更新日志

## v0.3.7

- 更新桌面、任务栏和侧边栏品牌图标，统一使用新版月夜主题图片。
- 修复并完善智慧职教（ICVE）测验/作业识别、答题与日志展示流程。
- 同步版本号到 v0.3.7。

## v0.3.5

- 默认主题升级为灰白黑半透明磨砂风格，统一卡片、侧边栏、按钮、表单、日志区和弹窗的玻璃态层次与阴影。
- 优化主题 token：新增主色对比色变量，避免不同主题下按钮文字对比异常。
- 优化设置页题库配置布局，减少按钮堆叠，保留预设、自定义、连通性测试和 OCS 配置导入能力。
- 新增通用第三方题库适配能力，支持按接口文档配置请求方法、请求体字段、请求头、答案路径和答案分割规则。
- 支持粘贴 OCS 题库配置并自动解析 URL、请求方法、Header、题目字段、选项字段和答案路径，便于接入常见题库。
- README 更新为正常中文说明，补充项目架构、功能状态、第三方题库和默认主题说明。
- 同步版本号到 v0.3.5。

## v0.3.4

- 课程进度页新增智慧职教（ICVE）支持：使用已保存 Cookie 登录后拉取课程和任务点进度。
- ICVE 课程进度按任务点 `Speed >= 100` 统计完成数，支持部分任务点拉取失败时保留课程列表并显示提示。
- 同步版本号到 v0.3.4。

## v0.3.3

- 智慧职教（ICVE）新增“自动获取 Cookie”流程：启动独立 Edge/Chrome 会话，用户主动登录后可一键读取该会话 Cookie 并回填账号表单。
- 自动获取 Cookie 使用独立临时浏览器用户目录，不读取用户原浏览器 Cookie。
- 新增 Wails 后端接口 `StartICVECookieCapture` / `ReadICVECookie`，用于启动 Cookie 捕获会话和读取登录后的 Cookie。
- 同步版本号到 v0.3.3。

## v0.3.2

- 智慧职教（ICVE）改为仅支持 Cookie 登录，不再误走账号密码验证码登录，避免出现“验证码验证失败”。
- 账号管理中选择智慧职教后，Cookie 输入标签旁新增“获取教程”跳转按钮，直达原项目 Cookie 获取教程。
- 智慧职教启动时跳过 OCR/Core Runtime 初始化，减少不必要的启动步骤。
- 同步版本号到 v0.3.2。

## v0.3.1

- 账号管理的视频模式下拉改为按平台动态展示，避免所有平台都显示学习通专属的“多课程并发 / 多任务点并发”。
- 切换平台时自动校正非法 `videoModel`，并显示当前平台的视频模式说明。
- 对齐 WeLearn 随行课堂视频模式：`1=刷学习时长`，`2=刷完成度`。
- 修复 WeLearn 学时模式只加学习时长、不推进完成状态的问题，补齐达标后的完成度提交流程。
- 修复 HQKJ 课程进度刷新在网络波动或内部 panic 时可能导致页面失败/闪退的问题，增加重试、recover 和部分失败提示。
- 修复课程进度页和账号管理页历史编辑残片导致的前端构建失败。
- 同步版本号到 v0.3.1。

## v0.3.0

- 重写 WeLearn 随行课堂桌面安全 runner，不再直接调用原 console 中容易 panic / log.Fatal 的流程。
- 修复 WeLearn 刷课时 `unexpected EOF` 导致 worker 静默停止的问题，登录、课程、章节、任务点和提交学时均加入重试与错误日志。
- 优化 WeLearn 课程进度拉取，章节或任务点局部失败时保留已获取课程，并显示部分失败状态。
- WeLearn 学时时长兼容桌面填写习惯，支持 `15`、`15分钟`、`15 min`、`15-30`、`15分钟-30分钟`。
- 新增 WeLearn 学时时长解析单元测试，防止后续回退为只支持原项目范围格式。
- 同步版本号到 v0.3.0。
