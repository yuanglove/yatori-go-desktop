# 更新日志

## v0.3.0

- 重写 WeLearn 随行课堂桌面安全 runner，不再直接调用原 console 中容易 panic / log.Fatal 的流程。
- 修复 WeLearn 刷课时 `unexpected EOF` 导致 worker 静默停止的问题，登录、课程、章节、任务点和提交学时均加入重试与错误日志。
- 优化 WeLearn 课程进度拉取，章节或任务点局部失败时保留已获取课程，并显示部分失败状态，避免课程列表不全或整页失败。
- WeLearn 学时时长兼容桌面填写习惯，支持 `15`、`15分钟`、`15 min`、`15-30`、`15分钟-30分钟`。
- 新增 WeLearn 学时时长解析单元测试，防止后续回退为只支持原项目范围格式。
- 同步版本号到 v0.3.0。

## v0.2.9178

- 同步原项目 `yatori-go-console` 最新海旗科技更新：支持海旗科技作业/考试 AI 自动答题逻辑。
- 优化账号管理、全局设置、课程进度等页面的下拉菜单动画与浮层显示，避免菜单被弹窗或卡片裁剪。
- 修复课程进度状态徽标换行问题，状态文本保持单行显示。
- 修复日志中心等级过滤：支持 DEBUG / INFO / WARN / ERROR 按等级过滤，并记住日志中心内手动选择的过滤等级。
- 补全 `CANGHUI` 仓辉实训平台入口，按英华兼容路径运行。
- 同步版本号到 v0.2.9178。

## v0.2.91

- 恢复学习通“免费AI / 学习通内置AI”答题选项，并改为红色叹号悬浮提示说明，避免挤压表单布局。
- 新增远程公告弹窗，支持本地已读记录，网络失败时静默跳过。
- 公告弹窗改为只有勾选“下次不再展示”才记录已读；仪表盘新增“公告”按钮，可随时手动打开当前公告。
- README 新增手动发布公告说明。
- 新增仓库根目录 `announcement.json`，作为默认远程公告内容。
- README 新增免责声明章节，说明项目仅供学习与技术交流使用，禁止倒卖和违法违规使用。
- 修复英华学堂同用户名不同入口账号配置保存冲突问题。
- 同步版本号到 v0.2.91。
## v0.2.9

- 重新设计桌面端整体 UI，统一侧边栏、卡片、表格、按钮、弹窗、日志中心和设置页视觉风格。
- 使用 lucide-react 统一图标系统，替换导航和按钮中的 emoji / 特殊字符。
- 重写“关于本项目”页面，拆分为版本检测、项目来源、功能说明、数据目录和安全声明区域，缓解页面拥挤问题。
- 保留 AI/题库失败后随机答选择题/判断题开关，账号管理中继续移除“免费AI”选项。
- 同步版本号到 v0.2.9。

## v0.2.8

- 优化学习通任务日志输出，减少启动后大量“跳过”、章节测验开关关闭、并发槽占用/释放等流水日志刷屏。
- 保留启动、登录、课程获取、课程过滤汇总、视频进度、实际答题、待做作业/考试、失败/错误等关键日志。
- 修复学习通多任务点并发控制，CxNode 仅控制同一账号内同时进行的视频任务点数量。
- 新增账号级“AI/题库失败后随机答选择题/判断题”开关，仅在已配置 AI 或题库但未获取有效答案时生效。
- 同步版本号到 v0.2.8。

## v0.2.7

- 新增课程进度页面，可按账号拉取课程列表并展示进度。
- 课程进度支持学习通、英华学堂、海旗科技、WeLearn 随行课堂。
- 课程进度支持课程名搜索、只看未完成、进度条、任务完成数/总数展示。
- 优化 worker 启动速度：海旗、WeLearn 等无需 OCR 的平台跳过 Core Runtime 初始化。
- 修复海旗课程进度适配，改为使用原核心 `HqkjGetNodeProgressAction` 拉取节点进度。
- 学习通课程进度补充任课老师、任务完成数、总任务数和 classId/key 展示。

## v0.2.5

- 修复英华学堂启动时因 OCR / ONNX Runtime 未初始化导致 worker 子进程异常退出的问题。
- 修复英华平台题相关流程进入前缺少 Core Runtime 初始化的问题。
- 英华任务启动时增加更完整的诊断日志，包括 URL、密码保存状态、Runtime 初始化状态和 panic 堆栈。
- 新增全局最大同时运行任务数设置，支持限制 worker 子进程并发。
- 启动时自动从 GitHub 检测新版本，发现新版后弹出提示，并提供“去更新”跳转按钮。
- 关于本项目页的手动版本检测增加“去更新”按钮。
- README 功能说明中补充已实测可用平台：学习通、英华学堂、海旗科技、WeLearn 随行课堂。

## v0.2.0

- 新增更多平台的桌面任务入口，统一通过 worker 子进程运行，避免平台核心异常直接关闭主窗口。
- 接入英华学堂桌面任务入口，支持视频、作业、考试等流程的 worker 执行。
- 新增主题系统，支持深色、白色、浅绿色、浅蓝色、柔粉色、暖米色、淡紫色、石墨灰、护眼青。
- 主题设置移动到全局设置页，切换后立即生效，并写入 `config.yaml` 持久化。
- 优化日志乱码处理，增加 Windows GBK mojibake 修复路径。
- 优化学习通章节测验答题逻辑：AI/题库已配置但选择题无有效答案时随机兜底，未配置时不答题。
- 保持任务停止为 worker 子进程硬停止。
- 更新关于本项目页面和版本号为 v0.2.0。

## v0.1.0

- 基于 [yatori-dev/yatori-go-console](https://github.com/yatori-dev/yatori-go-console) 改造为 Windows 桌面版。
- 增加 Wails v2 + React + TypeScript 前端。
- 增加账号管理、任务控制、日志中心、全局设置、关于本项目页面。
- 支持学习通任务启动与硬停止。
- 增加统一日志输出格式。
- 支持学习通多课程 / 多任务点模式配置。
- 支持章节测验 AI 答题配置。
- 增加原项目、本项目仓库、更新日志跳转按钮。
